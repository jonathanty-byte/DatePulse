// DateDetox V1 — Simplified types (no backend dependency)

export type { ScoreResult, ScoreLabel, HeatmapSlot, BestTimeSlot, NextPeak } from "../lib/scoring";
export type { AppName } from "../lib/data";
